numpy==1.23.5
pandas==1.5.3
openpyxl
ipykernel
jupyter
Pillow

# Install the version of Tensorflow depending on your processor: darwin == Mac, ARM == M1
tensorflow-macos==2.17.0; sys_platform == 'darwin' and 'ARM' in platform_version # Mac M-chips
tensorflow==2.17.0; sys_platform == 'darwin' and 'ARM' not in platform_version # Mac Intel chips
tensorflow==2.17.0; sys_platform != 'darwin' # Windows & Ubuntu Intel chips

# Testing packages
pytest
black
ruff

# API
fastapi # api framework
pytz # timezone library
uvicorn # server for fastapi
python-multipart  # for handling file uploads in FastAPI

# test
httpx #HTTP client
pytest-asyncio  # asynchronous I/O support for pytest